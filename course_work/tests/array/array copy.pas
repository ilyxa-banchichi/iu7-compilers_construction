program ArrayTest;

var
    a: array[1..10] of integer;
    ar: array[1..10] of real;
    i, n: integer;
    f: real;
begin
    // n := 10;
    // for i := 1 to n do begin
    //     a[i] := i;
    // end;

    // for i := 1 to n do begin
    //     writeln(a[i]);
    // end;
end.